app:
  name: CloudNest
  version: 1.0.0
  description: Personal Cloud Storage Platform

company:
  name: CloudNest Technologies

environment: development

# ── Global JWT Configuration ──────────────────────────────────
# Used by API Gateway (validation) and Auth Service (generation)
jwt:
  secret: "${JWT_SECRET:YnVnZmF0YWxzbm93bGVvcGFyZHN0cmV0Y2hpbmdvbmV0d29zbm93ZmFsbGluZ3RocmVlc2ZhbGxpbmc="
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}  # 24 hours default

# ── Global Datasource Defaults ───────────────────────────────
spring:
  jpa:
    hibernate:
      # validate: schema drift (e.g. a column missing from the table) fails
      # loudly at startup instead of surfacing as silent runtime 500s.
      # New environments need schema/migration scripts — see /database.
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd'T'HH:mm:ss.SSS'Z'

management:
  endpoints:
    web:
      exposure:
        include: health,info,env,metrics
  endpoint:
    health:
      show-details: when-authorized
  info:
    env:
      enabled: true

info:
  app:
    name: ${app.name}
    version: ${app.version}
    environment: ${environment}
